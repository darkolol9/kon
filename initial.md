i want to create a mysql cli tool, called kon, kon should allow me to run a few commands like kon connect, to set up a new connection (user gets a few promt question to enter host, port, connection name etc), kon ls, lists connections, kon set (connection wildcard matching by name), "kon" will open interactive SQL editor mode, (assuming an active connection is set), and it will allow user to write mysql code, and get results in a formatted table ish view, some features iwant are syntax highlighting, auto complete on syntax, and table names etc, 
